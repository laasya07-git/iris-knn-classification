# Iris Flower Classification using KNN

This project implements the K-Nearest Neighbors (KNN) algorithm to classify iris flowers based on their features.

## Tools Used
- Python
- Scikit-learn
- Pandas
- Jupyter Notebook
